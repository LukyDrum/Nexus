[package]
name = "nexusctl"
description = "A CLI tool for controlling the Nexus shell using commands."
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
clap.workspace = true
nexus-api = { path = "../nexus-api" }
tokio.workspace = true
