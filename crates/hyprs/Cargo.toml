[package]
name = "hyprs"
version = "0.1.0"
authors.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
tokio = { workspace = true, optional = true }

[features]
tokio = ["dep:tokio"]
